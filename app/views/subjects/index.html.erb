<h2>Subjects</h2>

<ul>
  <% @subjects.each do |subject| %>
    <li>
      <%= subject.title %> 
      <ul>
        <% find_all(subject).each do |card| %>
          <li><%= card.title %></li>
        <% end %>
      </ul>
    </li>
  <% end %>
</ul>